这是一个flow matching + RL的代码项目，启动方式和架构见@architecture.md

denoising step取不同值时（inference、train都取相同步数），训练效果如何？
denoising step在inference固定时，train不同时，上述结论如何变化？

value网络的输入是否该加入噪声？去掉会怎样？

action chunk是如何在原始action序列中取得？是用sliding window吗？

这是一个flow matching + RL的代码项目，启动方式和架构见@architecture.md
这里面支持两个算法，分别是reinflow和fpo。其中fpo算法不同于reinflow，它在中间没有任何加入噪声的部分，也就是完全没有“探索”，我有个新idea，fpo中初始噪声是否可以看作一种探索呢？如果需要探索时，就增加初始噪声的std，需要利用时则相反。

请帮我实现上述idea，具体而言，设置std_max=1.5，设置std_min=0.4。依据value网络的值来缓慢调整初始噪声的std，就像SAC算法中的熵奖励系数一样。查看当前batch的value值，std初始为1，value当接近1时，则缓慢增长std朝向std_max，反之则缓慢朝向std_min方向。

请帮我实现上述想法，实现这个算法，就减fpo_intt_std，仿照这个工作中fpo和reinflow的写法，作为一个新算法来实现，并创建配套的启动脚本，就像script/run_ft_steps_comparison_exp.sh，比较不同std_min/std_max下的实验效果。