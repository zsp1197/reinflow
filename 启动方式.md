这是一个flow matching + RL的代码项目，启动方式和架构见@architecture.md

denoising step取不同值时（inference、train都取相同步数），训练效果如何？
denoising step在inference固定时，train不同时，上述结论如何变化？

value网络的输入是否该加入噪声？去掉会怎样？

action chunk是如何在原始action序列中取得？是用sliding window吗？